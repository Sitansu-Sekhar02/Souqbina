<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/transparent"
    android:orientation="vertical">

    <com.leo.simplearcloader.SimpleArcLoader
        android:id="@+id/progress_dialog_loader"
        android:layout_width="85dp"
        android:layout_height="85dp"
        android:layout_centerInParent="true"
        android:visibility="visible"
        custom:arc_colors="@array/loader_colors"
        custom:arc_margin="3dp"
        custom:arc_speed="fast"
        custom:arc_style="simple_arc" />

    <ImageView
        android:id="@+id/progress_dialog_logo"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_centerInParent="true"
        android:background="@color/transparent"
        android:src="@mipmap/app_icon" />

    <com.sa.souqbinadriver.view.AppTextView
        android:id="@+id/progress_dialog_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/progress_dialog_loader"
        android:layout_centerHorizontal="true"
        android:layout_margin="15dp"
        android:paddingTop="20dp"
        android:text="@string/loading"
        android:textAppearance="@style/MediumText"
        android:textColor="@color/app_fontColor_dark" />
</RelativeLayout>

    <!-- <ProgressBar
            android:id="@+id/progress_dialog_loader"
            android:layout_width="75dp"
            android:layout_height="75dp"
            android:progressTint="@color/white"
            android:visibility="visible"
            android:layout_centerInParent="true"
            /> -->